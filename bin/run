#!/bin/bash

rootdir=$(cd $(dirname $0)/.. && pwd)
cd $rootdir
exitcode=1
while [[ ${exitcode} -ne 0 ]]; do
  mkdir -p logs
  nohup node_modules/electron/cli.js src/app.js >> logs/cron.log
  exitcode=$?
done
